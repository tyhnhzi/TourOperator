@page
@model TourOperatorWeb.Pages.Admin.QuanLyKhachSanModel
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Roles = "QuanTriHeThong")]
@{
    ViewData["Title"] = "Quản lý khách sạn";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h1 class="mb-1">Quản lý khách sạn</h1>
            <p class="text-muted mb-0">Danh sách khách sạn đối tác dùng cho các tour.</p>
        </div>
        <div>
            <a href="#" class="btn btn-primary js-open-hotel-modal" data-url="/Admin/KhachSan_Them">+ Thêm khách sạn</a>
        </div>
    </div>

    <table class="table table-striped table-hover align-middle">
        <thead class="table-light">
            <tr>
                <th>Tên khách sạn</th>
                <th>Địa chỉ</th>
                <th>Số điện thoại</th>
                <th style="width: 150px;">Thao tác</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var ks in Model.DanhSachKhachSan)
        {
            <tr>
                <td>@ks.TenKhachSan</td>
                <td>@ks.DiaChi</td>
                <td>@ks.SoDienThoai</td>
                <td>
                    <div class="btn-group btn-group-sm" role="group">
                        <a href="#" class="btn btn-outline-secondary js-open-hotel-modal" data-url="@Url.Page("/Admin/KhachSan_Sua", new { id = ks.Id })">Sửa</a>
                        <a href="#" class="btn btn-outline-danger js-open-hotel-modal" data-url="@Url.Page("/Admin/KhachSan_Xoa", new { id = ks.Id })">Xóa</a>
                    </div>
                </td>
            </tr>
        }
        </tbody>
    </table>

    <a asp-page="/Admin/TrangChuAdmin" class="btn btn-secondary mt-3">Quay lại trang quản trị</a>

    <!-- Modal hiển thị form Thêm/Sửa/Xóa khách sạn ngay trên trang danh sách -->
    <div class="modal fade" id="hotelModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Chi tiết khách sạn</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Đóng"></button>
                </div>
                <div class="modal-body p-0">
                    <iframe id="hotelModalFrame" src="" style="border:0; width:100%; height:500px;"></iframe>
                </div>
            </div>
        </div>
    </div>

    @section Scripts {
        <script>
            (function () {
                const modalElement = document.getElementById('hotelModal');
                const frame = document.getElementById('hotelModalFrame');
                if (!modalElement || !frame || !window.bootstrap) {
                    return;
                }

                const modal = new bootstrap.Modal(modalElement);

                document.querySelectorAll('.js-open-hotel-modal').forEach(function (btn) {
                    btn.addEventListener('click', function (e) {
                        e.preventDefault();
                        const url = this.getAttribute('data-url');
                        if (!url) return;
                        frame.src = url;
                        modal.show();
                    });
                });

                modalElement.addEventListener('hidden.bs.modal', function () {
                    frame.src = '';
                });
            })();
        </script>
    }
</div>
